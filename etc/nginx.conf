server {
        location /uploads/ {
            root /home/box/web/uploads;
        }
        location ~* .+\.(\w+)$ {
            /home/box/web/public;
        }
        location ~* (\w+)$ {
            return 404;
        }
    }

